{{- if .Values.manager.enabled }}
## Manager StatefulSet (v1.2.0 manager+worker mode)
## Owns all scheduling state. Dispatches pipeline runs to worker pods.
## Created only when manager.enabled=true; the standalone StatefulSet is skipped.
apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: {{ include "tram.fullname" . }}-manager
  labels:
    {{- include "tram.labels" . | nindent 4 }}
    app.kubernetes.io/component: manager
spec:
  replicas: 1
  serviceName: {{ include "tram.fullname" . }}-manager
  podManagementPolicy: OrderedReady
  updateStrategy:
    type: RollingUpdate
  selector:
    matchLabels:
      {{- include "tram.selectorLabels" . | nindent 6 }}
      app.kubernetes.io/component: manager
  template:
    metadata:
      annotations:
        {{- with .Values.podAnnotations }}
        {{- toYaml . | nindent 8 }}
        {{- end }}
        {{- if .Values.pipelines }}
        checksum/config: {{ include (print $.Template.BasePath "/configmap.yaml") . | sha256sum }}
        {{- end }}
      labels:
        {{- include "tram.selectorLabels" . | nindent 8 }}
        app.kubernetes.io/component: manager
    spec:
      serviceAccountName: {{ include "tram.serviceAccountName" . }}
      enableServiceLinks: false
      {{- if .Values.hostNetwork }}
      hostNetwork: true
      dnsPolicy: ClusterFirstWithHostNet
      {{- end }}
      securityContext:
        fsGroup: {{ .Values.securityContext.fsGroup | default 1000 }}
        {{- with .Values.securityContext.runAsUser }}
        runAsUser: {{ . }}
        {{- end }}
        {{- with .Values.securityContext.runAsGroup }}
        runAsGroup: {{ . }}
        {{- end }}
      {{- if .Values.postgresql.enabled }}
      ## Wait for PostgreSQL to accept connections before the manager starts.
      ## Prevents the "Could not initialise TramDB: connection refused" race on fresh installs.
      initContainers:
        - name: wait-for-postgres
          image: busybox:1.36
          command:
            - sh
            - -c
            - |
              echo "Waiting for PostgreSQL at {{ .Release.Name }}-postgresql:5432 ..."
              until nc -z {{ .Release.Name }}-postgresql 5432; do
                echo "PostgreSQL not ready — sleeping 3s"
                sleep 3
              done
              echo "PostgreSQL is up — starting manager"
      {{- end }}
      containers:
        - name: tram
          image: "{{ include "tram.managerImage" . }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          command: ["tram", "daemon"]
          ports:
            - name: http
              containerPort: {{ .Values.service.port }}
              protocol: TCP
          env:
            - name: TRAM_NODE_ID
              valueFrom:
                fieldRef:
                  fieldPath: metadata.name
            ## Manager mode: own all scheduling, dispatch runs to workers
            - name: TRAM_MODE
              value: "manager"
            ## D.2 (GH #17): count=1 stream durable placement. Default "1" (on) —
            ## set via .Values.env.TRAM_STREAM_SINGLE_PLACEMENT below (the env
            ## range injects it into this container).
            ##   "1" = count=1 streams get a durable 1-slot placement row
            ##   "0" = legacy count=1 single-dispatch path (rollback)
            ## E.2 (GH #21): queued manual runs. Default "1" (on) — a manual run
            ## triggered with no healthy workers is durably queued and dispatched
            ## when capacity returns; set via .Values.env.TRAM_QUEUE_MANUAL_RUNS
            ## below. "0" = legacy fail-fast (rollback).
            ## F.1: stateful transforms. Default "1" (on) — counter_delta and its
            ## durable per-pipeline state (transform_state table / internal API);
            ## set via .Values.env.TRAM_STATEFUL_TRANSFORMS below. "0" = disabled
            ## (rollback: validation rejects the transforms, endpoints 404).
            ## Point manager at its own service so workers can reach the callback URL
            - name: TRAM_MANAGER_URL
              value: "http://{{ include "tram.fullname" . }}:{{ .Values.service.port }}"
            ## Worker discovery via K8s headless DNS (tram-worker-0.tram-worker.<ns>.svc…)
            - name: TRAM_WORKER_REPLICAS
              value: {{ .Values.worker.replicas | quote }}
            - name: TRAM_WORKER_SERVICE
              value: "{{ include "tram.fullname" . }}-worker"
            - name: TRAM_WORKER_NAMESPACE
              valueFrom:
                fieldRef:
                  fieldPath: metadata.namespace
            - name: TRAM_WORKER_PORT
              value: {{ .Values.worker.port | quote }}
            {{- if gt (len .Values.pipelines) 0 }}
            - name: TRAM_PIPELINE_DIR
              value: /pipelines
            {{- end }}
            {{- range $key, $val := .Values.env }}
            - name: {{ $key }}
              value: {{ $val | quote }}
            {{- end }}
            {{- if hasKey (default dict .Values.envSecret) "TRAM_DB_URL" }}
            ## TRAM_DB_URL provided via envSecret — rendered exactly once by the
            ## envSecret range below; the chart's DB wiring defers to it entirely.
            {{- else if .Values.postgresql.enabled }}
            {{- $pgES := tpl (.Values.postgresql.auth.existingSecret | default "") . }}
            {{- if eq $pgES (include "tram.postgresSecretName" .) }}
            ## Auto-wired from the chart-managed postgres Secret (key: dbUrl) —
            ## the password is generated once and never interpolated into the pod spec.
            - name: TRAM_DB_URL
              valueFrom:
                secretKeyRef:
                  name: {{ include "tram.postgresSecretName" . }}
                  key: dbUrl
            {{- else if and (not $pgES) .Values.postgresql.auth.password }}
            ## Explicit postgresql.auth.password (no existingSecret) — dev path.
            - name: TRAM_DB_URL
              value: "postgresql+psycopg2://{{ .Values.postgresql.auth.username }}:{{ .Values.postgresql.auth.password }}@{{ .Release.Name }}-postgresql/{{ .Values.postgresql.auth.database }}"
            {{- else }}
            {{- fail "postgresql.enabled=true but TRAM_DB_URL cannot be resolved: leave postgresql.auth.existingSecret at the chart-managed default (password is generated and stored in a Secret), set postgresql.auth.password explicitly, or provide TRAM_DB_URL via envSecret." }}
            {{- end }}
            {{- else if .Values.manager.persistence.enabled }}
            ## SQLite on the manager RWO PVC — no external database required.
            - name: TRAM_DB_URL
              value: "sqlite:////data/tram.db"
            {{- end }}
            {{- if .Values.manager.persistence.enabled }}
            ## Schemas and MIBs on manager RWO PVC (/data).
            - name: TRAM_SCHEMA_DIR
              value: /data/schemas
            - name: TRAM_MIB_DIR
              value: /data/mibs
            {{- else if .Values.sharedStorage.enabled }}
            ## Shared RWX PVC covers schemas/MIBs when manager.persistence is disabled.
            - name: TRAM_SCHEMA_DIR
              value: /data/schemas
            - name: TRAM_MIB_DIR
              value: /data/mibs
            {{- end }}
            {{- if .Values.schemaRegistry.url }}
            - name: TRAM_SCHEMA_REGISTRY_URL
              value: {{ .Values.schemaRegistry.url | quote }}
            {{- end }}
            {{- if .Values.schemaRegistry.username }}
            - name: TRAM_SCHEMA_REGISTRY_USERNAME
              value: {{ .Values.schemaRegistry.username | quote }}
            {{- end }}
            {{- if .Values.schemaRegistry.password }}
            - name: TRAM_SCHEMA_REGISTRY_PASSWORD
              value: {{ .Values.schemaRegistry.password | quote }}
            {{- end }}
            {{- range $key, $ref := .Values.envSecret }}
            - name: {{ $key }}
              valueFrom:
                secretKeyRef:
                  name: {{ $ref.secretName }}
                  key: {{ $ref.secretKey }}
            {{- end }}
            {{- if .Values.apiKey }}
            - name: TRAM_API_KEY
              value: {{ .Values.apiKey | quote }}
            {{- end }}
            {{- if .Values.authUsers }}
            - name: TRAM_AUTH_USERS
              value: {{ .Values.authUsers | quote }}
            {{- end }}
            {{- if not .Values.ui.enabled }}
            - name: TRAM_UI_DIR
              value: ""
            {{- end }}
            {{- if .Values.tls.enabled }}
            - name: TRAM_TLS_CERTFILE
              value: /tls/tls.crt
            - name: TRAM_TLS_KEYFILE
              value: /tls/tls.key
            {{- end }}
          volumeMounts:
            {{- if gt (len .Values.pipelines) 0 }}
            - name: pipelines
              mountPath: /pipelines
              readOnly: true
            {{- end }}
            {{- if .Values.manager.persistence.enabled }}
            ## RWO PVC — SQLite DB + schemas + MIBs
            - name: manager-data
              mountPath: /data
            {{- else if .Values.sharedStorage.enabled }}
            ## RWX PVC — schemas + MIBs (no per-manager DB PVC)
            - name: data
              mountPath: /data
            {{- end }}
            {{- if .Values.keys.secretName }}
            - name: keys
              mountPath: {{ .Values.keys.mountPath | default "/secrets" }}
              readOnly: true
            {{- end }}
            {{- if .Values.tls.enabled }}
            - name: tls
              mountPath: /tls
              readOnly: true
            {{- end }}
          livenessProbe:
            httpGet:
              path: /api/health
              port: http
            initialDelaySeconds: 10
            periodSeconds: 30
          readinessProbe:
            httpGet:
              path: /api/ready
              port: http
            initialDelaySeconds: 5
            periodSeconds: 10
          {{- with .Values.manager.resources }}
          resources:
            {{- toYaml . | nindent 12 }}
          {{- else }}
          {{- with .Values.resources }}
          resources:
            {{- toYaml . | nindent 12 }}
          {{- end }}
          {{- end }}
      volumes:
        {{- if gt (len .Values.pipelines) 0 }}
        - name: pipelines
          configMap:
            name: {{ include "tram.fullname" . }}-pipelines
        {{- end }}
        {{- if and .Values.manager.persistence.enabled .Values.manager.persistence.existingClaim }}
        - name: manager-data
          persistentVolumeClaim:
            claimName: {{ .Values.manager.persistence.existingClaim }}
        {{- else if and .Values.sharedStorage.enabled (not .Values.manager.persistence.enabled) }}
        ## RWX PVC — only reachable when manager.persistence is disabled, matching
        ## the pvc.yaml creation guard. Workers never mount the RWX volume: they
        ## sync schemas/MIBs from the manager via sync_assets() into their emptyDir.
        - name: data
          persistentVolumeClaim:
            claimName: data-{{ include "tram.fullname" . }}
        {{- end }}
        {{- if .Values.keys.secretName }}
        - name: keys
          secret:
            secretName: {{ .Values.keys.secretName }}
            defaultMode: 0440
        {{- end }}
        {{- if .Values.tls.enabled }}
        - name: tls
          secret:
            secretName: {{ .Values.tls.secretName }}
        {{- end }}
      {{- with .Values.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.affinity }}
      affinity:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
  {{- if and .Values.manager.persistence.enabled (not .Values.manager.persistence.existingClaim) }}
  volumeClaimTemplates:
    - metadata:
        name: manager-data
        labels:
          {{- include "tram.labels" . | nindent 10 }}
          app.kubernetes.io/component: manager
      spec:
        accessModes: ["ReadWriteOnce"]
        resources:
          requests:
            storage: {{ .Values.manager.persistence.size | default "10Gi" }}
        {{- with .Values.manager.persistence.storageClass }}
        storageClassName: {{ . | quote }}
        {{- end }}
  {{- end }}
{{- end }}
