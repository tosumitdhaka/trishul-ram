apiVersion: v1
kind: Service
metadata:
  name: {{ include "tram.fullname" . }}
  labels:
    {{- include "tram.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
      {{- if .Values.service.nodePort }}
      nodePort: {{ .Values.service.nodePort }}
      {{- end }}
    {{- range .Values.service.snmpTrapPorts }}
    ## UDP port for snmp_trap source connector (matches pipeline port:)
    - port: {{ . }}
      targetPort: {{ . }}
      protocol: UDP
      name: snmp-trap-{{ . }}
    {{- end }}
  ## Pin each client IP to the same pod so uploaded schemas/MIBs (stored on
  ## per-pod PVCs) remain visible across page refreshes in cluster mode.
  sessionAffinity: ClientIP
  selector:
    {{- include "tram.selectorLabels" . | nindent 4 }}
